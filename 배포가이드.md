# 🌐 game.chancebot.kr 배포 가이드 (GitHub Pages)

폴더에 필요한 파일은 모두 준비돼 있습니다. 아래 순서대로만 진행하면 됩니다 (약 15분).

## 1. GitHub 저장소 만들기

1. github.com 로그인 → New repository
2. 이름 예: `memory-game` / **Public** / README 추가 안 함 → Create

## 2. 파일 업로드

터미널에서 게임 폴더로 이동 후:

```bash
cd ~/Workspace/기억력게임
git init
git add index.html questions.json CNAME crawler/ .github/ README.md
git commit -m "기억력 게임 첫 배포"
git branch -M main
git remote add origin https://github.com/<내아이디>/memory-game.git
git push -u origin main
```

git이 처음이면 터미널이 GitHub 로그인을 요구합니다. 안내를 따라 진행하세요.
(시작하기.command와 배포가이드.md는 API 키가 들어있으니 올리지 마세요 — 위 명령은 해당 파일을 제외합니다)

## 3. GitHub Pages 켜기

저장소 → Settings → Pages
- Source: **Deploy from a branch**
- Branch: **main** / **(root)** → Save

## 4. TMDB 키 등록 (매일 자동 갱신용)

저장소 → Settings → Secrets and variables → Actions → New repository secret
- Name: `TMDB_API_KEY`
- Secret: `c684218ca3959a5d7c7c20e3332e571f`

등록하면 `.github/workflows/daily-questions.yml`이 **매일 한국시간 새벽 5시**에 크롤러를 돌려 questions.json을 자동 갱신합니다. Actions 탭에서 "매일 문제 자동 갱신" → Run workflow로 즉시 테스트 가능.

## 5. 도메인 연결 (DNS)

chancebot.kr을 등록한 업체(가비아/후이즈/Cloudflare 등)의 DNS 관리에서 레코드 추가:

| 타입 | 호스트(이름) | 값 |
|---|---|---|
| CNAME | game | `<내아이디>.github.io.` |

## 6. GitHub에 커스텀 도메인 등록

저장소 → Settings → Pages
- Custom domain: `game.chancebot.kr` 입력 → Save
- DNS 전파 후(수 분~수 시간) **Enforce HTTPS** 체크

## 완료 확인

- https://game.chancebot.kr 접속 → 게임 화면
- 영화 스틸컷·배우 사진 표시 확인 (Pages는 서버라 questions.json 정상 로드됨)
- 다음날 새벽 5시 이후 문제가 바뀌는지 확인

## 참고

- 출석/난이도는 방문자 브라우저(localStorage)에 각자 저장 → 별도 서버 불필요
- GitHub Actions는 해외 IP라 멜론/네이버 크롤링이 막힐 수 있음 → 그 경우 가수는 내장 풀, 배우는 TMDB로 자동 대체되도록 해둠
- 문제 직접 추가: questions.json 편집 후 `git add questions.json && git commit -m "문제 추가" && git push`
